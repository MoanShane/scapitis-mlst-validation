# Pinned to exact versions used to generate the published results.
# Do NOT relax these to >= without re-validating against
# expected_output/comparison_metrics_expected.csv — see README for
# specific version-sensitive behaviours encountered during development
# (pandas "NA" string auto-conversion to NaN; minor scikit-learn
# differences in adjusted_rand_score / AgglomerativeClustering across
# versions).

biopython==1.79
numpy==1.21.6
scipy==1.7.3
scikit-learn==1.0.2
pandas==1.3.5
matplotlib==3.5.3
networkx==2.8.8
